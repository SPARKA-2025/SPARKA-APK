<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:label="SPARKA"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sparka_v0"
        tools:targetApi="31">
        <activity
            android:name=".ui.splashscreen.SplashScreen"
            android:exported="true"
            android:theme="@style/Theme.Sparka_v0">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.splashscreen.login.LoginActivity"/>
        <activity android:name=".ui.splashscreen.register.RegisterActivity" />
        <activity android:name=".ui.splashscreen.register.RegisterPlatActivity" />
        <activity android:name=".ui.splashscreen.parkiranmoment.DetailParkirActivity" />
        <activity android:name=".MainActivity"/>
    </application>

</manifest>